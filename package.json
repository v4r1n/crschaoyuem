{
  "name": "crs-yuem-kuen-system",
  "version": "0.2.0",
  "private": true,
  "description": "Local automated acceptance tests for CRS Yuem-Kuen System on Google Apps Script.",
  "scripts": {
    "test": "npm run test:contracts && npm run test:backend && npm run test:frontend",
    "test:contracts": "node --test tests/contracts/project-contract.test.js",
    "test:backend": "node --test tests/backend/*.test.cjs",
    "test:frontend": "playwright test",
    "test:frontend:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  },
  "engines": {
    "node": ">=20"
  }
}
